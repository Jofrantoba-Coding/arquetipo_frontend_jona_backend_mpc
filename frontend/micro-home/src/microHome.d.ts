declare module "container/UiHome" {
    import React, { ComponentProps } from "react";

    // Define the props for UiHome if needed, otherwise just declare the component
    export interface UiHomeProps {
        // Add any props here if needed
    }

    // Define and export the UiHome component
    export class UiHome extends React.Component<UiHomeProps> {}

    export default UiHome;
}